/** Mounts the region sandbox into a [data-region-sandbox] element. */
export function mountRegionSandbox(root) {
  console.log('[region-sandbox] mount', root.dataset.assets);
  // Filled in by later tasks: load assets, build scene, start loop.
}
